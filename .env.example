# htmz Environment Variables Example
# 🚨 WARNING: Client-side environment variables are NOT SECURE!
# 🚨 API keys stored here will be visible to all users in their browser.
# 🚨 Use server-side proxies for production. See docs/SECURITY.md
#
# This file is for DEVELOPMENT/TESTING ONLY with public APIs

# API Endpoints
API_BASE=https://api.github.com
STRIPE_API=https://api.stripe.com/v1
BACKEND_URL=https://your-backend.com/api

# API Keys and Secrets - 🚨 NEVER USE REAL SECRETS CLIENT-SIDE! 🚨
# These examples are for PUBLIC/TEST keys only or development proxies
GITHUB_TOKEN=ghp_development_token_public_repos_only
STRIPE_KEY=pk_test_publishable_key_safe_to_expose
API_KEY=development_key_or_use_server_proxy
AUTH_TOKEN=temporary_jwt_token_not_permanent_secret

# Configuration
NODE_ENV=development
DEBUG=true
TIMEOUT=30000

# User Preferences
DEFAULT_USER=octocat
ITEMS_PER_PAGE=20
THEME=dark

# Third-party Services
ANALYTICS_ID=UA-XXXXX-X
SENTRY_DSN=https://your-sentry-dsn
PUSHER_KEY=your_pusher_key

# Feature Flags
ENABLE_BETA_FEATURES=false
ENABLE_ANALYTICS=true
ENABLE_ERROR_REPORTING=false

# Example usage in HTML:
# <button hz-get="{{env.API_BASE}}/users/{{env.DEFAULT_USER}}">Get User</button>
# <form hz-post="{{env.BACKEND_URL}}/contact" hz-headers='{"Authorization": "Bearer {{env.AUTH_TOKEN}}"}'>